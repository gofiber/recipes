name: Build each GoFiber recipe

on:
  push:
    branches:
      - master
      - main
    paths-ignore:
      - "**.md"
  pull_request:
    branches:
      - '*'
    paths-ignore:
      - "**.md"

jobs:
  builds:
    strategy:
      matrix:
        go-version:
          - oldstable
          - stable
    runs-on: ubuntu-latest
    steps:
      -   name: Fetch Repository
          uses: actions/checkout@v3
      -   name: Install Go
          uses: actions/setup-go@v4
          with:
            go-version: ${{ matrix.go-version }}
      -   name: Run go vet for each dir with go.mod
          run: find . -name go.mod -print0 | xargs -0 -n1 -P4 bash -c 'cd $(dirname $0) && go vet ./...'
      -   name: Run go build for each dir with go.mod
          run: for f in $(find . -name go.mod); do (cd $(dirname $f); go build ./...); done
